let showGrid = true;
let numXGrid = 20;
let numYGrid = 10;


function setup(){
    createCanvas(500, 500)
    background(0)
    if(showGrid == true){
        drawGrid(numXGrid,numYGrid);  
    }
}

function draw(){
    
}

